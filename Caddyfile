# Caddy automatically gets and renews a free HTTPS certificate for whatever
# domain is listed here — no manual certbot steps needed. epapwp.in is the
# canonical domain; www redirects to it.

www.epapwp.in {
    redir https://epapwp.in{uri} permanent
}

epapwp.in {
    reverse_proxy frontend:80
}
